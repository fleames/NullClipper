<Window x:Class="Clipper.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NullClipper"
        Width="448"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Background="{StaticResource WindowBgBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="{StaticResource AppFont}"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Icon="pack://application:,,,/Assets/clipper-icon.png"
        SourceInitialized="OnSourceInitialized">
    <WindowChrome.WindowChrome>
        <!-- CornerRadius stays 0: PerMonitorV2 + WindowChrome rounding mis-hits at non-100% DPI. DWM rounds the frame instead. -->
        <WindowChrome CaptionHeight="44"
                      GlassFrameThickness="1"
                      ResizeBorderThickness="0"
                      CornerRadius="0"
                      UseAeroCaptionButtons="False"
                      NonClientFrameEdges="None"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontFamily" Value="{StaticResource IconFont}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <Border x:Name="Bd"
                                    Width="28"
                                    Height="28"
                                    CornerRadius="6"
                                    Background="{TemplateBinding Background}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#14FFFFFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#22FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CaptureCta" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="14" Padding="{TemplateBinding Padding}" BorderThickness="1">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#4C8DFF" Offset="0"/>
                                    <GradientStop Color="#3B82F6" Offset="0.45"/>
                                    <GradientStop Color="#2563EB" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#66B4FF" Offset="0"/>
                                    <GradientStop Color="#1D4ED8" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#5B97FF" Offset="0"/>
                                            <GradientStop Color="#3B82F6" Offset="0.4"/>
                                            <GradientStop Color="#1D4ED8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SegmentRadio" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd"
                                Background="Transparent"
                                CornerRadius="8"
                                Padding="12,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#14FFFFFF"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#4C8DFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="QuietLinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource DangerHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardFillBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource HairlineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>
        <Style x:Key="RecorderBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource FieldFillBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource HairlineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource EdgeHoverBrush}"/>
                </Trigger>
                <Trigger Property="Tag" Value="rec">
                    <Setter Property="BorderBrush" Value="#4A5D75"/>
                    <Setter Property="Background" Value="#101820"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SwitchRow" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                            <Border x:Name="Track"
                                    Grid.Column="1"
                                    Width="40"
                                    Height="22"
                                    CornerRadius="11"
                                    Background="{StaticResource SwitchOffBrush}"
                                    BorderBrush="{StaticResource EdgeHoverBrush}"
                                    BorderThickness="1"
                                    VerticalAlignment="Center"
                                    Margin="16,0,0,0">
                                <Border x:Name="Thumb"
                                        Width="16"
                                        Height="16"
                                        CornerRadius="8"
                                        Background="#E8EEF5"
                                        HorizontalAlignment="Left"
                                        Margin="2,0,2,0"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource SwitchOnBrush}"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="#4A5D75"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Track" Property="BorderBrush" Value="#5A6B80"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="Transparent"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Margin="4,2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1A2433"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsHighlighted" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#254569"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource FieldFillBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource HairlineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="36"/>
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton Grid.ColumnSpan="2"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Focusable="False"
                                                  ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter IsHitTestVisible="False"
                                                      Margin="12,8,8,8"
                                                      VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                                    <Path x:Name="Arrow"
                                          Grid.Column="1"
                                          Data="M 0 1 L 5 6 L 10 1"
                                          Stroke="{StaticResource MutedTextBrush}"
                                          StrokeThickness="1.5"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          IsHitTestVisible="False"/>
                                </Grid>
                            </Border>
                            <Popup Placement="Bottom"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade"
                                   VerticalOffset="6">
                                <Border Background="{StaticResource SurfaceBrush}"
                                        BorderBrush="{StaticResource HairlineBrush}"
                                        BorderThickness="1"
                                        CornerRadius="12"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        Padding="2,6"
                                        Margin="0,0,10,12">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="6" Opacity="0.42" Direction="270" Color="#000000"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="240">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource EdgeHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4A5D75"/>
                                <Setter TargetName="Arrow" Property="Stroke" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource FieldFillBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource HairlineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource EdgeHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4A5D75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource QuietTextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="HintText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource QuietTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <Border x:Name="TitleBar"
                DockPanel.Dock="Top"
                Background="{StaticResource TitleBarFillBrush}"
                Height="44"
                WindowChrome.IsHitTestVisibleInChrome="True"
                MouseLeftButtonDown="OnTitleBarMouseDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                    <Border Width="22"
                            Height="22"
                            CornerRadius="6"
                            Background="#141C26"
                            BorderBrush="{StaticResource HairlineBrush}"
                            BorderThickness="1">
                        <Image Source="pack://application:,,,/Assets/clipper-icon.png"
                               Width="14"
                               Height="14"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                    <TextBlock Text="NullClipper"
                               Margin="10,0,0,0"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <Button x:Name="MinimizeButton"
                        Grid.Column="1"
                        Style="{StaticResource CaptionButton}"
                        Content="&#xE921;"
                        ToolTip="Hide to tray"
                        Click="OnMinimize"/>
            </Grid>
        </Border>
        <Border DockPanel.Dock="Top"
                Height="1"
                Background="{StaticResource HairlineBrush}"/>

        <Border DockPanel.Dock="Bottom"
                BorderBrush="{StaticResource HairlineBrush}"
                BorderThickness="0,1,0,0"
                Padding="20,12,20,16">
            <Button x:Name="ExitButton"
                    Style="{StaticResource QuietLinkButton}"
                    Content="Quit NullClipper"
                    Click="OnQuit"/>
        </Border>

        <StackPanel Margin="20,18,20,8">
            <Button x:Name="CaptureButton"
                    Style="{StaticResource CaptureCta}"
                    Click="OnCaptureNow">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="36"
                            Height="36"
                            CornerRadius="10"
                            Background="#28FFFFFF">
                        <TextBlock Text="&#xE722;"
                                   FontFamily="{StaticResource IconFont}"
                                   FontSize="15"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="12,0,12,0" VerticalAlignment="Center">
                        <TextBlock x:Name="CaptureTitle"
                                   Text="Capture"
                                   FontSize="14"
                                   FontWeight="SemiBold"/>
                        <TextBlock x:Name="CaptureSubtitle"
                                   Text="Region, window, or full screen"
                                   FontSize="11"
                                   Opacity="0.82"
                                   Margin="0,1,0,0"/>
                    </StackPanel>
                    <TextBlock Grid.Column="2"
                               Text="&#xE76C;"
                               FontFamily="{StaticResource IconFont}"
                               FontSize="12"
                               Opacity="0.85"
                               VerticalAlignment="Center"/>
                </Grid>
            </Button>

            <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                <Border BorderBrush="{StaticResource HairlineSoftBrush}"
                        BorderThickness="1"
                        CornerRadius="13"
                        Padding="16">
                    <StackPanel>
                        <TextBlock Text="MODE"
                                   Style="{StaticResource SectionLabel}"/>
                        <Border Background="{StaticResource FieldFillBrush}"
                                BorderBrush="{StaticResource HairlineBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="3"
                                Margin="0,10,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <RadioButton x:Name="SnipModeButton"
                                             Style="{StaticResource SegmentRadio}"
                                             Content="Snip"
                                             GroupName="CaptureKind"
                                             Click="OnSnipMode"/>
                                <RadioButton x:Name="GifModeButton"
                                             Grid.Column="1"
                                             Style="{StaticResource SegmentRadio}"
                                             Content="GIF"
                                             GroupName="CaptureKind"
                                             Click="OnGifMode"/>
                            </Grid>
                        </Border>
                        <TextBlock x:Name="CaptureModeHint"
                                   Style="{StaticResource HintText}"
                                   Text="GIF records a region at 12 fps for up to 8s. Longest side capped at 640px."
                                   TextWrapping="Wrap"
                                   Margin="2,8,0,0"/>
                    </StackPanel>
                </Border>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,12,0,0">
                <Border BorderBrush="{StaticResource HairlineSoftBrush}"
                        BorderThickness="1"
                        CornerRadius="13"
                        Padding="16">
                    <StackPanel>
                        <TextBlock Text="HOTKEY"
                                   Style="{StaticResource SectionLabel}"/>
                        <Border x:Name="HotkeyRecorder"
                                Style="{StaticResource RecorderBorder}"
                                Height="44"
                                Margin="0,10,0,0"
                                MouseLeftButtonDown="OnHotkeyClick">
                            <DockPanel Margin="12,0">
                                <Ellipse x:Name="RecordingDot"
                                         DockPanel.Dock="Left"
                                         Width="7"
                                         Height="7"
                                         Fill="{StaticResource MutedTextBrush}"
                                         Margin="0,0,10,0"
                                         Visibility="Collapsed"/>
                                <StackPanel x:Name="HotkeyChips"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center"/>
                            </DockPanel>
                        </Border>
                        <TextBlock x:Name="HotkeyHint"
                                   Style="{StaticResource HintText}"
                                   Text="Click, then press the keys you want."
                                   Margin="2,8,0,0"/>
                    </StackPanel>
                </Border>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,12,0,0" Padding="16,14">
                <CheckBox x:Name="StartupBox"
                          Style="{StaticResource SwitchRow}"
                          Content="Start NullClipper with Windows"
                          Click="OnStartupToggled"/>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,12,0,0">
                <Border BorderBrush="{StaticResource HairlineSoftBrush}"
                        BorderThickness="1"
                        CornerRadius="13"
                        Padding="16">
                    <StackPanel>
                        <DockPanel LastChildFill="True" Margin="0,0,0,14">
                            <Border DockPanel.Dock="Left"
                                    Width="32"
                                    Height="32"
                                    CornerRadius="9"
                                    Background="#161D26"
                                    BorderBrush="{StaticResource HairlineBrush}"
                                    BorderThickness="1"
                                    Margin="0,0,12,0">
                                <TextBlock Text="&#xE71B;"
                                           FontFamily="{StaticResource IconFont}"
                                           FontSize="13"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="NullImage"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextBrush}"/>
                                <TextBlock Text="Encrypted snips with a shareable link."
                                           Style="{StaticResource HintText}"
                                           Margin="0,1,0,0"/>
                            </StackPanel>
                        </DockPanel>
                        <CheckBox x:Name="NullImageEnabledBox"
                                  Style="{StaticResource SwitchRow}"
                                  Content="Enable NullImage uploads"
                                  Click="OnNullImageEnabledToggled"/>
                        <StackPanel x:Name="NullImageOptions">
                            <Border Height="1"
                                    Background="{StaticResource HairlineBrush}"
                                    Margin="0,14"/>
                            <TextBlock Text="EXPIRES"
                                       Style="{StaticResource SectionLabel}"/>
                            <ComboBox x:Name="NullImageExpiryBox"
                                      Margin="0,8,0,0"
                                      SelectionChanged="OnNullImageExpiryChanged">
                                <ComboBoxItem Content="1 hour" Tag="1h"/>
                                <ComboBoxItem Content="1 day" Tag="1d"/>
                                <ComboBoxItem Content="3 days" Tag="3d"/>
                                <ComboBoxItem Content="7 days" Tag="7d"/>
                            </ComboBox>
                            <Border Height="1"
                                    Background="{StaticResource HairlineBrush}"
                                    Margin="0,14"/>
                            <CheckBox x:Name="NullImageBurnBox"
                                      Style="{StaticResource SwitchRow}"
                                      Content="Burn after first view"
                                      Click="OnNullImageBurnToggled"/>
                            <Border Height="1"
                                    Background="{StaticResource HairlineBrush}"
                                    Margin="0,14"/>
                            <TextBlock Text="PASSWORD"
                                       Style="{StaticResource SectionLabel}"/>
                            <Grid Margin="0,8,0,0">
                                <PasswordBox x:Name="NullImagePasswordBox"
                                             PasswordChanged="OnNullImagePasswordChanged"/>
                                <TextBlock x:Name="PasswordWatermark"
                                           Text="Optional — protects each upload"
                                           Foreground="{StaticResource QuietTextBrush}"
                                           FontSize="12"
                                           IsHitTestVisible="False"
                                           VerticalAlignment="Center"
                                           Margin="12,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Border>
        </StackPanel>
    </DockPanel>
</Window>
